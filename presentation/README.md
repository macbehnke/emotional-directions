# Class Presentation

Generated deck:

```text
emotional_directions_class_presentation.pptx
```

Rebuild:

```bash
cd presentation
npm install
npm run build
```

The deck is built from `deck_data.json`, which summarizes the final result
tables. The PowerPoint file contains editable text and shapes.
